{
  "name": "base44-wrapper",
  "version": "1.0.0",
  "description": "Capacitor wrapper for Base44 app with OneSignal push",
  "main": "index.js",
  "scripts": {
    "build": "echo \"No web build needed, using remote URL\"",
    "test": "echo \"No tests\" && exit 0"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "onesignal-cordova-plugin": "^3.3.3"
  }
}
